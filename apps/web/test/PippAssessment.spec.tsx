import { render,screen } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { describe,expect,it,vi } from 'vitest';import { PippAssessment } from '../src/features/assessment/PippAssessment';
vi.mock('../src/api/client',()=>({evaluateAssessment:vi.fn()}));
describe('PippAssessment',()=>{it('keeps gestational selection independent and shows mismatch without blocking',async()=>{render(<PippAssessment infantContext={{gestationalWeeks:30,preterm:true,ventilated:false,chronicPain:false,postoperative:false,assessmentType:'acute'}}/>);await userEvent.click(screen.getByLabelText(/۳۶ هفته یا بیشتر/));expect(screen.getByText('هشدار ناسازگاری سن حاملگی')).toBeVisible()});it('renders percentage controls as accessible sliders',()=>{render(<PippAssessment/>);expect(screen.getByRole('slider',{name:/درهم کشیدن ابروها/})).toHaveAttribute('aria-valuetext')})});

import { render,screen } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { describe,expect,it,vi } from 'vitest';import { WorkflowStepper } from '../src/components/WorkflowStepper';
describe('WorkflowStepper',()=>{it('allows reached steps but disables future steps',async()=>{const onStep=vi.fn();render(<WorkflowStepper current={2} maxReached={2} onStep={onStep}/>);expect(screen.getByRole('button',{name:/مداخله/})).toBeDisabled();await userEvent.click(screen.getByRole('button',{name:/زمینه بالینی/}));expect(onStep).toHaveBeenCalledWith(1)})});
